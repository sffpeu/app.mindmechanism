# M13 Mechanism App

Quickstart

- Requirements: Node 20.x
- Install: `npm ci`
- Configure env: copy `.env.example` to `.env.local` and fill values
- Run dev: `npm run dev`

Docs

- See `RULES.md` for project conventions and guardrails
- See `CONTRIBUTING.md` for workflow and PR checklist
- See `SECURITY.md` for secrets and Firestore rules guidance
